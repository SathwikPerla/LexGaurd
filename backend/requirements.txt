# LEXGUARD Backend — Python Dependencies
# All versions pinned to exact releases for reproducibility and security.
# Python >= 3.11 required (uses match statements, modern typing).

# ── Web framework ──────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.30.1       # includes httptools + uvloop for performance
python-multipart==0.0.9         # required for FastAPI UploadFile handling

# ── Data validation (Pydantic v2 ONLY) ────────────────────────────────────
pydantic==2.7.1
pydantic-settings==2.3.0        # for Settings model + env var validation

# ── Document parsing ───────────────────────────────────────────────────────
PyMuPDF==1.27.2.3               # PDF text extraction; binary wheels available for arm64
python-docx==1.1.2              # DOCX parsing

# ── Structured logging ─────────────────────────────────────────────────────
python-json-logger==2.0.7       # JSON-formatted structured log output

# ── Testing ────────────────────────────────────────────────────────────────
pytest==8.2.1
pytest-asyncio==0.23.7
httpx==0.27.0                   # TestClient transport for FastAPI
# reportlab removed — tests use PyMuPDF (fitz) directly to create test PDFs

# ── Dev utilities ──────────────────────────────────────────────────────────
python-dotenv==1.0.1

# ─── Step 2 — LLM + Local Embeddings + Vector DB ─────────────────────────
anthropic==0.40.0                  # Anthropic SDK: Claude for all agents
sentence-transformers==3.3.1       # Local embeddings via all-MiniLM-L6-v2 (no API cost)
tenacity==9.0.0                    # Retry with exponential backoff for API calls
chromadb==0.5.20                   # Local vector DB — pinned to 0.5.x (1.x breaks API)

# ─── Step 3+ (uncomment when ready) ───────────────────────────────────────
# google-cloud-vision==3.7.3        # Cloud Vision OCR
# google-cloud-storage==2.17.0      # GCS document storage
# langgraph==0.1.13                 # Multi-agent pipeline (direct, no LangChain wrappers)
